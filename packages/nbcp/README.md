# lotterymcp

`lotterymcp` 是 Lotterymcp 的对外短命令入口包。

它用于把福彩3D、排列3、双色球、大乐透、排列5、七星彩、快乐8的真实历史数据接入支持 MCP 的 AI 工具。

安装后可以直接使用：

```bash
npx --yes lotterymcp@latest
```

或者：

```bash
npm i -g lotterymcp
lotterymcp
```

它内部会转发到正式 CLI 实现包 `neuxnbcp`。
